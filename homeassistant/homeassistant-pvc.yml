apiVersion: v1
kind: PersistentVolumeClaim
metadata:
 name: homeassistant
 namespace: homeassistant
spec:
  storageClassName: local-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
